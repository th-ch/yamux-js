export enum EVENTS {
    ACCEPT = 'accept',
    CONNECT = 'connect',
    ERROR = 'error',
}
export const EVENT_ACCEPT = EVENTS.ACCEPT;
export const EVENT_CONNECT = EVENTS.CONNECT;
export const EVENT_ERROR = EVENTS.ERROR;
